# Layout trashing demo

[Live demo](https://mmelnikau.github.io/layout-trashing-demo/)

If you run app locally, please use `demo` script:

```bash
npm run demo
```

## Github page

If you want to deploy app to Github page, you must build the app first, and next use the `deploy` script.

```bash
npm run build && npm run deploy
```
